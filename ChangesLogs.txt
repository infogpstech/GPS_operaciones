# ChangesLogs - GPS Operations Suite (GOS)

## [v0.1.0] - 2025-05-24
### Inicialización y Migración
- Migración de archivos de GPSpedia v2.0 a `legacy/gpspedia/`.
- Preparación de la estructura raíz para GOS.
- Implementada estructura base de GOS en `index.html`.
- Implementada lógica de ruteo y navegación básica en `main.js`.
- Configurada `api-config.js` para consumir microservicios legacy.
- Implementado núcleo de GOS Core en `services/gos/gos-core.js`.
- Implementada gestión de Órdenes y Agenda.
- Integrada geolocalización en tiempo real.
- Estructurada integración con Drive y sistema de estadísticas.
- Implementada configuración dinámica de Agenda desde Spreadsheet.
- Desarrollado Motor de Asignación Avanzado con detección de segundos trabajos.
- Implementada lógica de proximidad para detección automática de llegada.
- Creado sistema de notificaciones base en el backend.
- Configurada PWA funcional (manifest.json y service-worker.js).
- Refinada UI de Órdenes con tablas dinámicas y estados visuales.
- Implementado módulo de Consulta Técnica simplificada (GOS <-> GPSpedia).
- Implementado Directorio de Clientes y Panel de Técnicos con seguimiento.
- Actualizada versión visible de la aplicación a v0.3.0.
- Implementado renderizado dinámico de la Agenda con vinculación de órdenes.
- Desarrollado CRUD de Clientes con formularios de registro.
- Implementado sistema de notificaciones visuales (Toasts) y persistencia en backend.
- Corregido error de sintaxis en `gos-core.js` (declaración duplicada de `ordersSheet`).
- Refinada UI global con layouts sticky y componentes de alta densidad.

## [v0.3.1] - 2025-05-24
### Despliegue y Verificación de Microservicio
- Implementada función `doGet` en `services/gos/gos-core.js` (Línea 400) para permitir verificación de despliegue mediante navegador.
- Actualizado endpoint `GOS_CORE` en `api-config.js` (Línea 7) con el URL de despliegue final.

## [v0.3.2] - 2025-05-24
### Auditoría Integral y Sincronización
- Actualizado endpoint `GOS_CORE` en `api-config.js` con el nuevo URL de producción.
- Realizada Auditoría Integral de todas las fases (Fase 1-3).
- Documentada hoja de ruta para GOS v0.4.0 en `Instrucciones.txt`.
- Incrementada versión global a v0.3.2.

## [v0.3.3] - 2025-05-24
### Refinamiento Operativo y Reportes
- Implementada UI de Reportes (Diario, Semanal, Mensual) en `main.js`.
- Expandido formulario de "Nueva Orden" con todos los campos requeridos por el README.
- Implementada lógica de filtrado temporal para reportes en el backend (`gos-core.js`).
- Automatizada la asignación de técnicos inmediatamente después de la creación de una orden.

## [v0.3.4] - 2025-05-24
### Auditoría de Arquitectura y Roadmap v0.4.0
- Realizada auditoría línea por línea de `main.js` identificando áreas de refactorización arquitectónica (Líneas 100-600).
- Realizada auditoría de `services/gos/gos-core.js` identificando dependencias de columnas posicionales (Líneas 30, 65, 155, 169).
- Documentados hallazgos técnicos en `Auditoria.txt`.
- Refinados objetivos del Roadmap v0.4.0 en `Instrucciones.txt`.

## [v0.4.0] - 2025-05-24
### Normalización Estructural y Refactorización Arquitectónica
- **Backend (`services/gos/gos-core.js`):**
  - Implementada utilidad `getHeaderMap` para eliminar dependencias de índices de columna fijos (Línea 33).
  - Refactorizadas todas las funciones CRUD y operativas para usar mapeo de encabezados dinámico.
  - Implementado sistema de IDs persistentes `getNextId` basado en contador en la hoja de configuración (Línea 51).
  - Rediseñada la gestión de configuración `handleGetSystemConfig` para soportar estructura tabular por categorías (Línea 218).
  - Mejorada la función `findOrCreateSheet` con inicialización de encabezados y formato automático (Línea 12).
- **Frontend (`main.js`):**
  - Implementado objeto `AppState` para gestión centralizada de estado, sesión y configuración global (Línea 75).
  - Implementado motor de plantillas `UI_TEMPLATES` para desacoplar el HTML de la lógica de renderizado (Línea 7).
  - Refactorizados formularios (Órdenes, Reportes) para poblar elementos `<select>` dinámicamente desde la configuración del backend (Línea 560).
  - Migrada la lógica de geolocalización y cambio de estado para consumir el estado global unificado.
- **Limpieza y Estabilización:**
  - Eliminados archivos de respaldo `.bak` y scripts temporales de verificación.
  - Verificada la integridad del sistema mediante simulaciones en Playwright.

## [v0.4.1.1] - 2025-05-24
### Mejoras Funcionales y Expansión de Módulos (Roadmap v0.4.0)
- **Reportes Avanzados (main.js, style.css):**
  - Implementada visualización gráfica mediante barras CSS en el módulo de Reportes (Línea 640).
  - Añadida funcionalidad de exportación a CSV para reportes operativos (Línea 675).
- **Asignación e Interactividad (main.js):**
  - Implementado modal interactivo de confirmación para asignación de segundos trabajos (Línea 405).
  - Refinada la lógica de detección de llegada: ahora solo se activa si la orden está en estado "En Camino" (Línea 515).
- **Integración de Mapas y Drive (index.html, main.js, gos-core.js):**
  - Integrado script de Google Maps API (placeholder) y lógica de seguimiento de técnicos en tiempo real (Línea 700).
  - Parametrizada la raíz de Google Drive en el backend, consumiendo 'rootFolderId' desde la configuración tabular (Línea 150).
- **Estabilización de Verificación:**
  - Expuesto el motor de plantillas `UI_TEMPLATES` al objeto global `window` para facilitar pruebas automatizadas (Línea 30).
  - Actualizada la versión visible de la aplicación a v0.4.1 en index.html y metadatos.

## [v0.4.1.2] - 2025-05-24
### Correcciones Críticas y Normalización de Datos
- **Backend (gos-core.js):**
  - Implementada normalización de claves Unicode (NFD) para eliminar acentos y caracteres especiales en las respuestas JSON (Línea 505).
  - Asegurada la consistencia de nombres de propiedades entre backend y frontend.
- **Frontend (main.js):**
  - Eliminado script de Google Maps hardcodeado en index.html a favor de la carga dinámica (Línea 145).

## [v0.4.2] - 2025-05-24
### Mejoras de Arquitectura y Preparación para Producción
- **Backend (gos-core.js):**
  - Implementada función `initializeSystem` para creación automática de catálogos y hojas de auditoría (Línea 35).
  - Refactorizada la normalización de claves Unicode (NFD) para asegurar consistencia en todo el sistema (Línea 185, 510, 540).
  - Implementado sistema de logs centralizado `logToSheet` para Auditoría y Logs de errores (Línea 455).
  - Rediseñada la jerarquía de Drive: Año > Mes > Orden > [Sub-carpetas] (Línea 485).
- **Frontend (main.js):**
  - Expandido el formulario de Clientes con campos RTN, Correo, Empresa y Observaciones (Línea 490).
  - Implementada visualización de colores por estado en la Agenda sincronizada con la configuración (Línea 585).
  - Añadidas acciones de integración directa con Drive y Google Maps desde la tabla de órdenes (Línea 645, 785).
  - Sincronizada la versión global a v0.4.2 en index.html y metadatos.

## [v0.5.0] - 2025-05-24
### Dashboard Inicial, Recepción Inteligente, Comprobante y Portal Seguro de Clientes
- **Backend (services/gos/gos-core.js):**
  - Modificada la inicialización `initializeSystem` para crear hojas `Sectores`, `Usuarios_Sectores` y `RecepcionVehiculos` (Líneas 35-50).
  - Agregado pre-poblado automático de sectores y usuarios-sectores por defecto (Líneas 51-78).
  - Expandida la creación de órdenes `handleCreateOrder` con campos `Vendedor`, `Color`, `Fecha Instalacion` y generación automática de un `Token` UUID de seguridad (Líneas 191-230).
  - Implementados manejadores `handleGetUserSector` (Líneas 655-690), `handleUpdateUserSector` (Líneas 691-720) y `handleSaveVehicleReception` (Líneas 721-758).
  - Implementado parseador de nombres `parseName` (Líneas 761-771) and el manejador seguro de consulta de cliente `handleGetClientPortalData` (Líneas 772-888).
  - Mapeadas las nuevas acciones en `doPost` (Línea 936).
- **Frontend (main.js):**
  - Declarado `dashboardInterval` global (Línea 2).
  - Modificada la carga de secciones en `loadSection` para soportar el Dashboard operativo (Líneas 287-320).
  - Implementado el renderizador de métricas y filtros del sector en `renderDashboardModule` (Líneas 849-1035).
  - Implementado el formulario inteligente en `renderVehicleReceptionForm` (Líneas 1036-1514).
  - Implementado el Comprobante Digital con código QR de consulta en `renderDigitalReceipt` (Líneas 1515-1695).
  - Actualizado `showMainView` para cargar automáticamente el sector operativo del usuario (Líneas 770-790).
  - Normalizadas las comparaciones de estado para remover acentos utilizando NFD (Línea 971).
- **Frontend (portal.html):**
  - Creado Portal de Consulta seguro y móvil-responsivo para visualización pública de datos de órdenes mediante token (Líneas 1-300).
- **Frontend (style.css, index.html):**
  - Agregado enlace de navegación para Dashboard en index.html (Línea 40).
  - Incorporados estilos para el Dashboard, campos autocompletados, pins responsivos de daños, Comprobante de Recepción, reglas de impresión @media print y maquetación móvil del Portal de Clientes (Líneas 304-560).

### Integración de Tickets, Estados de Instalación, Firma Digital, Historial del Vehículo y Dashboard de Roles
- **Backend (services/gos/gos-core.js):**
  - Añadida siembra automática de catálogos (`TiposTrabajo`, `Estados`, `Prioridades`, `Servicios`, `Turnos`) en `initializeSystem` para configurar el Spreadsheet operativo (Líneas 77-142).
  - Expandido `handleCreateOrder` y `handleUpdateOrderStatus` para admitir los campos `"Clasificación Vehículo"` y `"Firma Digital"` (Líneas 218-245, 312-358).
  - Implementado `handleGetVehicleHistory` para realizar búsquedas por número de chasis (VIN) y recuperar el historial de servicios completo (Líneas 246-284).
  - Modificado `handleGetClientPortalData` para exponer de forma segura el estado de firma digital del cliente (Líneas 969-1008).
- **Frontend (main.js):**
  - Actualizado el formulario de orden para incluir la clasificación mandatoria y un receptor de evento `blur` en el VIN (Líneas 82-120).
  - Desarrollada la consulta de historial automática que autocompleta campos, dibuja tablas de servicios previos, y advierte al asistente de ventas si el vehículo ya posee un dispositivo GPS (Líneas 702-811).
  - Creado el método `UI_TEMPLATES.priorityBadge` para ocultar todo texto de prioridad y mostrar únicamente círculos de color (Normal = Verde, Alta = Naranja, Máxima = Rojo) (Líneas 24-35).
  - Modificado `renderDashboardModule` para filtrar vistas por roles: vista de planificación semanal para gerencia y vista de lista sectorizada con botones de transición paso a paso (`Iniciando`, `Instalando`, `Haciendo pruebas`, `Instalación completada`) para técnicos (Líneas 1012-1322).
  - Implementado panel `renderAdminMetricsModule` restringido para visualizar tarjetas administrativas sólo con permisos privilegiados (Líneas 1970-2059).
  - Desarrollado lienzo de firma digital `initSignatureCanvas` y el formulario de confirmación de entrega `renderPostInstallationForm` (Líneas 2060-2200).
  - Añadidas vistas imprimibles para el Ticket Pre-instalación `renderTicketPreView` y la Evidencia Post-instalación `renderTicketPostView` (Líneas 2201-2500).
- **Frontend (index.html, style.css):**
  - Incorporada navegación `nav-admin-metrics` oculta por defecto y expuesta vía RBAC (Línea 47).
  - Diseñados estilos responsivos para el lienzo de firma, controles de impresión `@media print` y bordes de prioridad visual (Líneas 760-805).

## [v0.5.1] - 2025-05-24
### Dashboard de Agenda Operativa y Gestión de Cupos
- **Frontend (main.js):**
  - Modificado `UI_TEMPLATES.orderForm` para cambiar el campo de hora por un select dinámico (`#order-hora`) y añadir el botón para guardar como borrador (`#save-draft-btn`) (Líneas 85-147).
  - Reescripto `renderAgendaModule` para implementar un calendario dinámico rodante de 8 columnas a partir del día de hoy local del usuario (Líneas 591-804).
  - Desarrollada la función `window.bookSlot` para pre-poblar el formulario de nueva orden al pulsar directamente un cupo disponible en la agenda (Líneas 805-824).
  - Creada la función `updateSlots` que calcula la disponibilidad en tiempo real aplicando reglas de horarios normales y extraordinarios, límites de reserva y validez temporal de borradores de 15 minutos (Líneas 877-1011).
- **Frontend (style.css):**
  - Diseñado un layout responsivo mediante CSS Grid de 8 columnas para el calendario semanal (Líneas 87-250).
  - Incorporadas reglas visuales y de interacción para tarjetas de cupos (`card-confirmed`, `card-draft`, `card-available`, `card-available-partial`) con colores suaves, transparencias y bordes específicos (Líneas 87-250).
- **Frontend (index.html):**
  - Actualizada la versión visible a v0.5.1 (Línea 56).

## [v0.5.2] - 2025-05-24
### Refinamiento de Dashboard y Flujo de Trabajo Operativo
- **Frontend (style.css):**
  - Reducida la tipografía global en body a 14px (~1pt de reducción) para un diseño compacto y optimizado (Línea 14).
  - Reducidos los márgenes y paddings excesivos en tablas, botones, formularios y contenedores generales (Líneas 14, 33, 50, 77, 292, 303).
- **Frontend (main.js):**
  - Añadidas restricciones de rol para Técnicos: ocultado el botón Agenda del navbar y protegida la navegación directa en `loadSection` redirigiendo al Dashboard (Líneas 317, 1092).
  - Filtrada la tabla principal en Gestión de Órdenes para Técnicos, mostrando únicamente los trabajos correspondientes a su división (Línea 822).
  - Implementado visor de agenda por pestañas/tabs para Asistentes de Ventas, permitiendo cambiar de división para consulta sin alterar su división principal (Línea 597).
  - Implementada visibilidad inteligente en la Agenda semanal: la columna Domingo y slots extraordinarios permanecen ocultos a menos que haya citas programadas o se posea rol de administrador (Línea 597).
  - Añadida validación de capacidad (4 cupos en turnos regulares, 1 extraordinario bajo prioridad Alta/Máxima) al guardar órdenes (Línea 1085).
  - Implementada la función helper de duraciones estimadas `calculateEstimatedDuration` para instalaciones y diagnósticos derivados (Línea 1270).
  - Desarrollada la detección automática de retrasos y reasignación inteligente con prompts de interacción (Línea 1550).
  - Implementado el flujo operativo del técnico (Moviéndose al lugar, Se recibió el vehículo, Vehículo no disponible con opciones de Alistamiento o Taller y medición visual de tiempos de espera de 40 mins) (Línea 1550).
- **Backend (services/gos/gos-core.js):**
  - Modificado `handleUpdateOrderStatus` para admitir `usuario` y `observaciones` y registrar cada transición en `Historial_Estados` (Línea 197).
  - Implementado `checkAndUpdateJobDelays` para monitorizar la duración de trabajos activos y marcarlos como "Trabajo retrasado" (Línea 1134).
  - Implementado `handleReassignNextJob` para reasignar automáticamente trabajos posteriores a técnicos libres en caso de retraso prolongado (Línea 1134).
- **Frontend (index.html):**
  - Sincronizada la versión visible del sistema a v0.5.2 (Línea 56).

## [v0.6.0] - 2025-05-24
### Sistema de Roles, Privilegios, Administración y Control Operativo
- **Backend (services/gos/gos-core.js):**
  - Implementada gestión completa de usuarios para Jefes de División y Desarrolladores (Líneas 1100-1300).
  - Implementadas restricciones por división: los Jefes de División únicamente pueden gestionar usuarios de su propia división (Líneas 1100-1300).
  - Añadido mecanismo seguro de cambio de contraseña para técnicos y usuarios generales (Líneas 1240-1280).
  - Desarrollado motor mensual de horas extras calculando fórmulas exactas para L-V (antes de 8AM/después de 5PM), Sábados (antes de 8AM/después de 12PM) y Domingos (todo el día) (Líneas 1315-1440).
  - Implementada compensación automática de almuerzo de 60 minutos al finalizar tarde por retrasos, desplazando y reorganizando de forma automática los trabajos de agenda subsecuentes (Líneas 1445-1600).
- **Frontend (main.js):**
  - Modificado el ruteo global en `loadSection` para soportar el nuevo panel consolidado de `"admin"` en lugar de `"admin-metrics"` (Líneas 340-390, 420-460).
  - Implementada la vista "Administración" con pestañas funcionales para Métricas, Desempeño, Usuarios, Configuración y Horas Extras (Líneas 2830-3050).
  - Desarrollada la generación de gráficos SVG vectoriales nativos para líneas comparativas (instalaciones por mes) y circulares de distribución de ventas (advisors y técnicos) (Líneas 2720-2825).
  - Ocultados y protegidos lógicamente todos los botones de control de cupos ('Crear', 'Cancelar', 'Autorizar') para usuarios sin permisos adecuados utilizando la utilidad de permisos `RBAC` (Líneas 950-1050, 1100-1200).
  - Implementado visor de estado técnico y panel de cambio de contraseñas seguro desde información personal (Líneas 1680-1720, 1835-1875).
- **Frontend (index.html):**
  - Cambiada la sección nav-admin-metrics a nav-admin ("Administración") (Líneas 47-49).
  - Sincronizada la versión global visible a v0.6.0 (Línea 56).

## [v0.7.0] - 2025-05-24
### Geolocalización, Persistencia, Gestión de Cupos, Arquitectura de Datos y Separación de Servicios GOS
- **Backend (services/gos/gos-core.js):**
  - Añadida inicialización automática de la hoja `UbicacionesGuardadas` con cabeceras correctas (Líneas 35-50).
  - Desarrollados controladores de ubicaciones guardadas (`handleGetSavedLocations`, `handleSaveSavedLocation`) con ordenamiento y prioridad por usos (Líneas 1680-1760).
  - Desarrollado contador dinámico de técnicos activos por división para regular la capacidad de cupos permitidos por turno (Líneas 1761-1785).
- **Frontend (main.js):**
  - Añadido mecanismo Screen Wake Lock API (`navigator.wakeLock.request`) para evitar suspensión de PWA durante operaciones activas en curso (Líneas 6-25, 410-430).
  - Implementada detección automática de finalización de instalación al retirarse del lugar de trabajo (>300 metros) después de realizar las pruebas con monitoreo, actualizando estado a "Instalación completada" (Líneas 330-345).
  - Modificado el formulario de órdenes para incluir autocompletado y sugerencias inteligentes de ubicaciones guardadas y priorizadas (Líneas 140-160, 1080-1150).
  - Integrado selector interactivo de mapas (Google Maps API) en el formulario para capturar con un clic coordenadas geográficas exactas, dirección y enlace Maps (Líneas 1151-1225).
  - Modificado el flujo de guardado para registrar o incrementar el uso de la ubicación guardada en la base de datos (Línea 1500).
  - Modificada la validación de cupos en el formulario de órdenes para consultar la capacidad dinámicamente según la cantidad de técnicos activos de la división (Línea 1475).
- **Frontend (index.html):**
  - Sincronizada la versión de compilación visible del sistema a v0.7.0 (Línea 56).

## [v0.7.1] - 2025-05-24
### Bloque Doceavo de Nuevas Implementaciones y Actualizaciones
- **index.html:**
  - Agregados los CDN de hojas de estilo (Línea 9) y scripts (Línea 10) para la integración de Leaflet Map.
  - Renombrada la opción de navegación de "Dashboard" a "Planificación de la semana" (Línea 44) y reordenado el navbar para colocar "Reportes" adyacente a esta.
- **style.css:**
  - Agregado el estilo para `#welcome-msg` (Línea 26) para reducir el tamaño de fuente y compactar el saludo del encabezado.
  - Agregado el estilo para `header h2` (Línea 31) para reducir el espaciado vertical.
- **main.js:**
  - Modificado el enrutador en `showMainView` (Líneas 1640-1660) para establecer 'agenda' como la sección inicial para todos los roles de usuario.
  - Removido el redireccionamiento para técnicos en `loadSection` (Líneas 465-475) permitiéndoles el acceso libre a la Agenda.
  - Reubicada toda la información estadística consolidada y gráficos administrativos (`renderAdminMetricsModule`) dentro de la pestaña `Reportes` (`renderReportsModule`) (Líneas 511-540) para roles privilegiados.
  - Implementado el filtrado de agenda semanal para ocultar domingos y slots de sábados tarde por defecto, y reactivar domingo en caso de reservas activas (Líneas 871-875).
  - Añadido el select dinámico en `#order-hora` (Línea 178) con soporte de slots normales y extraordinarios.
  - Implementados los botones de agendamiento extraordinario (+) en `renderAgendaModule` (Líneas 941-1055) y el método global `bookExtraordinarySlot` (Líneas 1090-1110) para pre-rellenar datos.
  - Añadida la regla de negocio para restringir el guardado de extraordinarios a Borrador para Asesores en `renderOrderForm` y el validador de envío (Líneas 1224-1275, 1603-1610).
  - Implementada la pre-carga automática de fecha libre disponible con `initDateAutocomplete` (Línea 1214) al abrir el formulario.
  - Añadido el banner `#retraso-warning-msg` (Línea 267) y el calculador dinámico `checkRetrasoWarning` (Línea 1332) para alertas de retraso de 10+ minutos con asignaciones subsecuentes.
  - Reemplazado el campo 'Contacto' por 'Dirección del cliente' (Línea 194).
  - Integrado el mapa abierto de Leaflet en el formulario de creación de órdenes para captura interactiva de coordenadas, geocodificación reversa de direcciones vía Nominatim, detección de duplicados en radio de 50-100m, y parseo automático de enlaces Google Maps (Líneas 1420-1530).
  - Ocultados de la vista del usuario los campos coordenadas, división, enlace y removida categoría del vehículo.

## [v0.8.0] - 2025-05-24
### Bloque Catorceavo de Formulario Unificado, Gestión de Borradores e Integración de Autocompletados Inteligentes
- **services/gos/gos-core.js:**
  - Implementada la función `checkAndExpireDrafts` (Línea 1810) que localiza borradores con más de 60 minutos transcurridos tras su inicio y actualiza su estado a "Expirada", registrando la transición en `Historial_Estados`.
  - Integrada la invocación automática de `checkAndExpireDrafts` en el punto de obtención de órdenes `handleGetOrders` (Línea 400).
- **main.js:**
  - Actualizado el motor de plantillas `UI_TEMPLATES.orderForm` (Líneas 220-305) para unificar la orden de trabajo y sustituir la selección de servicios por casillas de verificación de Tracklink y Controlcar, y agregar el dropdown condicional para el Tipo de servicio.
  - Modificado el gestor `window.bookSlot` (Línea 1145) para presentar un selector interactivo del tipo de trabajo (overlay modal) antes de abrir el formulario, autocompletando la opción elegida.
  - Implementado el ajuste de expiración y vigencia de borradores a 60 minutos en la lógica de filtrado de Agenda semanal y en el validador de envío (Líneas 922, 1892).
  - Añadido controlador de cambio para `#order-anio` (Línea 1960) para mostrar u ocultar dinámicamente el campo Placa.
  - Implementados los autocompletados en tiempo real para el campo de Cliente y Marca del vehículo (Líneas 1980-2100), con carga automatizada de la información de contacto y validación de inventario condicional para clientes con Empresa.
  - Integrada la lógica de rellenado automático de campos de vehículos en el formulario cuando el cliente tiene un único registro en el historial o selección guiada mediante formato "Marca | Modelo | Placa | Chasis" cuando tiene múltiples unidades.
  - Rediseñada la validación programática de envío en el listener `submit` de `#order-form` (Línea 2195) para validar rigurosamente campos obligatorios según el modo de guardado (Borrador vs. Confirmación).

## [v0.9.0] - 2025-05-24
### Bloque Quinceavo de Separación entre Orden de Instalación y Orden de Trabajo, Validación de VIN, Gestión de Firmas y Campos Internos Sanitizados
- **services/gos/gos-core.js:**
  - Modificada la inicialización del sistema `initializeSystem` (Líneas 35-50) para crear de forma automática las hojas `Ordenes_Trabajo` y `Firmas_Tecnicos` y registrar los parámetros de configuración por defecto para numeración híbrida (`OT_Base_Number` en `1000`, `OT_Generation_Method` en `automatic` y `OT_Auto_Numeration` en `active`).
  - Actualizado `handleCreateOrder` (Línea 357) para utilizar el prefijo `"OI-"` en la autogeneración de identificadores secuenciales de Órdenes de Instalación.
  - Implementados métodos del backend para la gestión de numeración de OTs (`handleGetOTConfig` / `handleUpdateOTConfig`) y administración de firmas de técnicos (`handleSaveTechnicianSignature` / `handleGetTechnicianSignature` / `handleDeleteTechnicianSignature` / `handleGetSavedSignatures`).
  - Desarrollada la función `handleCreateOT` (Línea 1857) que realiza la validación manual de VIN de 17 caracteres contra la OI comercial correspondiente y clona toda la información para inicializar una nueva Orden de Trabajo única.
  - Actualizado `handleSaveVehicleReception` (Línea 905) para persistir las evidencias visuales, anotaciones de daños y fotos directamente en la hoja `Ordenes_Trabajo`.
  - Modificado `handleGetClientPortalData` (Línea 1026) para consultar la información de la Orden de Trabajo y retornar únicamente las funcionalidades instaladas visibles para el cliente, excluyendo datos técnicos de uso interno (IMEI, SIM y tipos de apagado remoto).
  - Registradas todas las nuevas rutas operativas en el conmutador global de `doPost` (Línea 2084).
- **main.js:**
  - Actualizado `UI_TEMPLATES.orderForm` (Línea 218) para incluir campos dedicados de Nombre y Teléfono de la persona de contacto que entregará el vehículo al técnico.
  - Modificada la función `showOrderDetailsModal` (Línea 5083) para visualizar todos los datos comerciales detallados de la OI (instalación local/remota, número de motor, inventario, persona de contacto y teléfono).
  - Incorporado el modal de confirmación y validación manual de chasis (VIN) en el botón "Se recibió el vehículo" (Línea 2951) que crea de forma dinámica la OT ligada tras la comprobación de correspondencia.
  - Desarrollado el lienzo interactivo de firmas en Recepción y Entrega, permitiendo registrar o auto-aplicar firmas de técnicos tras confirmación de contraseña, y habilitar la opción administrativa de eliminación de firmas.
  - Implementado el panel de ingreso exclusivo de IMEI, SIM principal, segundo GPS y selección de funcionalidades instaladas en el modal "Iniciar Pruebas con Monitoreo" (Línea 3089), persistiendo los cambios de forma automática en la OT.
  - Actualizados los layouts de tickets Pre-instalación y Post-instalación (Líneas 5298, 5312) para diagramar y contrastar de manera unificada la Orden de Instalación (OI) y de Trabajo (OT) con sus códigos QR de portal cliente respectivos.
- **portal.html:**
  - Modificada la renderización pública del cliente (Línea 203) para incorporar un bloque visualmente sanitizado de funcionalidades instaladas conformes a la seguridad de la información.


## [v1.0.0] - 2026-07-31
### Nuevas Implementaciones, Rediseño de Agenda y Motor de Planificación Inteligente
- **style.css:**
  - Añadido el estilo de clase `.extra-slot-btn` (Línea 255) para botones de agendamiento extraordinario simplificados.
  - Añadida la clase `.card-locking` (Línea 275) para estados de bloqueo temporal con fondo café claro.
  - Actualizados los colores y estilos de `.card-available` (Línea 227) para mostrar color verde claro y `.card-confirmed` (Línea 188) para mostrar fondo celeste suave.
- **services/gos/gos-core.js:**
  - Añadida la inicialización de las hojas `"Indisponibilidad"`, `"Ordenes_Trabajo"` y `"Firmas_Tecnicos"` de forma automática en `initializeSystem()` (Línea 48).
  - Implementado el método `handleLockSlot()` (Línea 2167) para reservar temporalmente un cupo en la hoja de órdenes con status 'Reservando' y timestamp.
  - Implementado el método `handleUnlockSlot()` (Línea 2229) para limpiar o remover el bloqueo de cupos de la hoja de órdenes.
  - Modificado `checkAndExpireDrafts()` (Línea 2252) para realizar un barrido hacia atrás que expire borradores antiguos de más de 60 minutos y limpie bloqueos de más de 5 minutos de antigüedad de forma automática.
  - Refactorizado `handleGetDivisionTechniciansCount()` (Línea 1805) para calcular dinámicamente la capacidad del turno basándose en los técnicos activos y restando indisponibilidades y ausencias parciales.
  - Implementados los métodos `handleSaveUnavailability()` (Línea 2831) y `handleGetUnavailability()` (Línea 2845) para gestionar registros de técnicos ausentes.
  - Implementado el método `handleUpdateOrderTechnician()` (Línea 2865) para guardar asignaciones técnicas preferenciales desde el panel de Jefes.
  - Desarrollada la función `handleLogPerformance()` (Línea 2596) para registrar de forma automática la duración del trabajo, puntualidad, retrasos, promedios por vehículo (marca, modelo, rango de años) y tipo de ubicación en la hoja `Log_Rendimiento` al finalizar un trabajo.
  - Desarrollado el método `handleGetTechnicianMetrics()` (Línea 2700) para calcular dinámicamente resúmenes de rendimiento para el panel de Jefatura.
- **services/gos/despachador.js:**
  - Creado y estructurado un módulo independiente (Línea 1) para las funciones matemáticas de despacho inteligente, optimización de rutas con velocidad promedio de 60 km/h, factores de tráfico, cálculo de duraciones estimadas y compensaciones de almuerzo.
- **main.js:**
  - Implementada la inicialización y persistencia local de IndexedDB (`initIndexedDB`, `dbSet`, `dbGetAll`, `dbClear`) (Línea 5) para almacenar y recuperar órdenes, clientes y configuraciones de forma instantánea y fluida.
  - Simplificados los botones de agendamiento extraordinario en `renderAgendaModule()` (Línea 1020) sustituyendo textos descriptivos por un símbolo "+". Ocultados los extraordinarios de los domingos.
  - Implementado el menú desplegable (Drop Menu) semi-transparente y autolimpiable (`showDropMenu`) (Línea 1142) anclado a los botones "Reservar" y "+", eliminando el uso de modales.
  - Refactorizado `renderAgendaModule()` (Línea 935) para realizar actualizaciones y sincronizaciones completamente silenciosas en segundo plano, comparando datos nuevos con la caché de IndexedDB y parchando únicamente las tarjetas modificadas en el DOM.
  - Rediseñada la función `renderDashboardModule()` (Línea 2866) para soportar almacenamiento en IndexedDB y silent updates sin parpadeos, recargas visuales o pérdida de foco.
  - Integrada la lógica de bloqueo temporal de cupos en `window.bookSlot()` (Línea 1218) que bloquea el cupo inmediatamente e informa si se encuentra reservado por otro usuario.
  - Añadida la regla de desbloqueo automático de cupos temporales en `loadSection()` (Línea 558) al cambiar de sección o cancelar el formulario.
  - Implementado el escalado dinámico de colores en `renderAgendaModule()` (Líneas 1098, 1135) mediante interpolación de RGB que gradualmente desplaza las tarjetas de Reservado y Confirmado hacia rojo conforme transcurra el tiempo sin recepción, alcanzando rojo puro a los 40 minutos.
  - Ajustado el mapa interactivo del formulario de creación de órdenes (`#form-map-picker`) (Línea 265) a una relación de aspecto cuadrada (1:1).
  - Desarrollada la detección y parseo automático de enlaces de Google Maps, coordenadas geográficas y direcciones en el campo de ubicación unificado (`savedLocInput`) (Línea 1814).
  - Enriquecida la vista detallada del técnico (`showOrderDetailsModal`) (Línea 5425) mostrando un mapa Leaflet de aspecto cuadrado de la ubicación de la instalación que abre Google Maps app en el dispositivo al hacer clic.
  - Removidas limitaciones rígidas de longitud de VIN (placeholders y maxlength) (Líneas 276, 3109, 3496) para permitir longitudes variables.
  - Añadida la visualización e integración de fotografías del historial vehicular directamente en la sección de Memoria Histórica del Vehículo en el formulario de creación de órdenes (Línea 2007).
  - Desarrollados los formularios de indisponibilidad temporal (ausencias parciales con hora de retorno) y asignación preferente de técnicos con visualizador de agenda semanal en el panel de configuración (Líneas 4900, 5060).

## [v1.1.0] - 2026-07-31
### Reorganización del Repositorio, Protección de Backend, Motor de Asignación Avanzado, Hashing de Contraseñas y Módulo de Migración
- **Reorganización Estructural:**
  - Creada la estructura modular de carpetas: `Legacy/`, `Interfaces/HTML/`, `Interfaces/vistas/`, `Interfaces/componentes/`, `Motores de negocio/`, `Backend/`, `Recursos/`, y `Utilidades/`.
  - Trasladados todos los microservicios y archivos legacy de GPSpedia (auth, catalog, feedback, users, utilities, write, imagen) bajo el directorio `Legacy/`.
  - Reubicados todos los recursos gráficos y logotipos a `Recursos/`.
  - Movida la hoja de estilos a `Interfaces/vistas/style.css` y el portal público a `Interfaces/HTML/portal.html`.
- **Protección de Credenciales y Abstracción (Backend):**
  - Modificado `services/gos/gos-core.js` (Línea 6) para abstraer `SPREADSHEET_ID`, `GPSPEDIA_SPREADSHEET_ID` y `ROOT_FOLDER_ID` a través de un objeto centralizado `CONFIG` que recupera los parámetros mediante `PropertiesService.getScriptProperties()`.
  - Implementada una capa de abstracción de persistencia `Database` (DAO/Repository Pattern) en `services/gos/gos-core.js` (Línea 21) para encapsular el acceso directo al Spreadsheet.
- **Motor de Asignación y Clasificación (Backend/despachador.js):**
  - Añadida la clasificación automática de trayectorias en `classifyTravel` en `services/gos/despachador.js` (Línea 12) y `services/gos/gos-core.js` (Línea 3043) para segregar el trabajo entre: Trabajo Local, Viaje en moto y Viaje largo.
  - Implementado el sistema de rotación equitativa en `services/gos/gos-core.js` (Líneas 3067, 3140) registrando desplazamientos especiales de forma individual en la hoja de `Historial_Viajes_Especiales`.
  - Implementada la prevención de conflictos de adyacencia de agenda en `hasAdjacentTravelConflict` (Línea 1252) y reorganización automática en `handleAutoAssignTechnical` (Línea 642).
- **Módulo de Migración y Consola Técnica:**
  - Creado el panel interactivo de migración de desarrollador `migracion.html` (Línea 1) en el directorio raíz, con asistentes de validación, simulador, logs en consola técnica y documentación SQL/NoSQL completa.
- **Seguridad y Hashing:**
  - Modificado el almacenamiento y verificación de contraseñas de usuarios en `services/gos/gos-core.js` (Líneas 592, 1418, 1473, 1542, 1575) para utilizar algoritmo hash seguro SHA-256 en lugar de texto plano, incluyendo retrocompatibilidad de contingencia.
- **Respaldos Automáticos:**
  - Implementado el módulo de respaldos automáticos y versionamiento `BackupManager` y `BackupStorageDestination` en `services/gos/gos-core.js` (Líneas 21, 2411) para serializar y salvaguardar información de tablas de forma desacoplada en el Drive.
- **Front-end (main.js):**
  - Extraídas las utilidades de cache IndexedDB y helpers globales a `Utilidades/helpers.js` (Línea 1) y actualizados las importaciones en `main.js` e `index.html`.
  - Integrado el aviso de alerta de viaje en moto de 30 minutos antes (Línea 2863) en la interfaz operativa de técnicos y la validación automática de coordenadas en el inicio de recorrido (Línea 417).

## [v1.1.1] - 2026-07-31
### Solución de Menú Desplegable y Bloqueo de Turnos Caducados
- **Limpieza del Repositorio:**
  - Removido el archivo duplicado y obsoleto `Backend/gos-core.js` (Línea 1) tras auditar y confirmar que carecía de referencias en el codebase y que era idéntico a `services/gos/gos-core.js`.
- **Selector de Tipo de Trabajo (main.js):**
  - Modificada la función `proceedWithBooking()` (Línea 1273) en `main.js` para registrar warnings pero continuar la ejecución si ocurre una falla en `lockSlot`, asegurando que el formulario unificado se cargue de forma continua en cualquier entorno.
  - Optimizada la población de campos de agendamiento (Fecha, Hora, Tipo de Trabajo) para ejecutarse de forma inmediata en el flujo de renderizado y con re-intentos controlados mediante `setTimeout` (Líneas 1332-1335) para eliminar condiciones de carrera.
- **Validación de Turnos Caducados (main.js):**
  - Implementada la función global `isSlotExpired()` (Línea 777) para evaluar la expiración de turnos a partir de la fecha y hora locales del sistema.
  - Actualizado el renderizado de la agenda en `drawAgenda()` (Líneas 1003, 1015, 1073) para deshabilitar turnos transcurridos que no tengan órdenes confirmadas, renderizándolos como bloqueados con el texto "Turno Vencido" (sin emoticonos) y removiendo botones `+` y eventos de clic.
  - Agregada validación de seguridad de turnos caducados en el envío del formulario `'order-form'` (Línea 2394) y en `bookSlot`/`bookExtraordinarySlot` (Líneas 1308, 1408) para bloquear cualquier agendamiento o borrador en franjas ya transcurridas.
  - Implementada la re-evaluación automática de día y re-estructuración de columnas de la agenda semanal en el temporizador en segundo plano (Líneas 1217-1224) para mantener la coherencia temporal sin requerir recarga manual.
